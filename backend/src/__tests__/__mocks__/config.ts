export const config = {
  NODE_ENV: 'test',
  PORT: 3001,
  DATABASE_URL: 'postgres://test:test@localhost:5432/test',
  JWT_SECRET: 'a'.repeat(32),
  JWT_EXPIRES_IN: '8h',
  REDIS_URL: 'redis://127.0.0.1:6379',
  REDIS_MAX_RETRIES: 3,
  DB_POOL_MAX: 5,
  DB_POOL_IDLE_TIMEOUT: 20_000,
  DB_POOL_CONNECTION_TIMEOUT: 5_000,
  WEBHOOK_QUEUE_CONCURRENCY: 2,
  WEBHOOK_QUEUE_MAX_RETRIES: 3,
  WEBHOOK_QUEUE_BACKOFF_MS: 100,
  WEBHOOK_BATCH_SIZE: 5,
  WEBHOOK_BATCH_INTERVAL_MS: 100,
  WEBHOOK_RATE_LIMIT_PER_MIN: 600,
  API_RATE_LIMIT_PER_MIN: 300,
  CLUSTER_WORKERS: 0,
  DEDUP_CACHE_TTL_SECS: 60,
  ZAPIER_WEBHOOK_SECRET: 'test-secret-123',
  ZENDESK_WEBHOOK_SECRET: undefined,
  DEEL_API_KEY: undefined,
  DEEL_API_BASE_URL: 'https://api.deel.com',
};

export type Config = typeof config;
